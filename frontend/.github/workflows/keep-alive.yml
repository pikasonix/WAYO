name: Keep Server Alive

on:
  # Run every 5 days
  schedule:
    - cron: '0 0 */5 * *'
  # Allow manual trigger
  workflow_dispatch:

jobs:
  ping:
    name: Keep Function Warm
    runs-on: ubuntu-latest
    
    steps:
      - name: Send HTTP Request
        run: |
          curl -X POST https://jrxpkakimcdfecdepmyu.supabase.co/functions/v1/rebuild-website \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImpyeHBrYWtpbWNkZmVjZGVwbXl1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDM4NzAwODMsImV4cCI6MjA1OTQ0NjA4M30.F-NJrY02GF6BqtdDeoWufJJEX0UrZivPdo8AwFRz6Qc" \
            -d '{}' \
            || echo "Ping sent" 